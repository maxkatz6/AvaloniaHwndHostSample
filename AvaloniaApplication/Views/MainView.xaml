<UserControl x:Class="AvaloniaApplication.Views.MainView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="400"
             mc:Ignorable="d">

  <StackPanel Margin="20"
              VerticalAlignment="Center"
              Spacing="20">
    <TextBlock HorizontalAlignment="Center" Text="{Binding Greeting}" />
    <TextBox HorizontalAlignment="Center" Text="{Binding EditingText, Mode=TwoWay}" />
    <Polygon HorizontalAlignment="Center"
             Fill="{DynamicResource SystemControlBackgroundAccentBrush}"
             Points="0,0 20,15, 0,30"
             Stroke="{DynamicResource SystemControlHighlightListAccentHighBrush}"
             StrokeThickness="5" />
  </StackPanel>
</UserControl>
